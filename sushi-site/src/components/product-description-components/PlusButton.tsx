import React from "react"
import styled from "styled-components";
import { Image } from "../common-components/Image/Image";

interface IPlusButtonProps {
    onClick?: () => void;
}

export const PlusButton = styled(Image)<IPlusButtonProps>`
`

